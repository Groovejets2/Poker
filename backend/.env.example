# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (SQLite for test/dev)
DB_PATH=./data/test/poker.db

# Production PostgreSQL Configuration (CRIT-5)
# Only used when NODE_ENV=production
# DB_HOST=your-postgres-host.com
# DB_PORT=5432
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_NAME=openclaw_poker
# DB_SSL_CA=/path/to/ca-certificate.crt

# JWT Secret (REQUIRED - CRIT-1)
# Generate a secure secret with: openssl rand -base64 32
# NEVER use a weak secret or commit the real secret to version control
# Example generated secret (DO NOT USE THIS - generate your own):
# JWT_SECRET=Kx7+9mP3nQ5sT8vW0yZ2aB4cD6eF8gH0iJ2kL4mN6oP8qR0sT2uV4wX6yZ8aB0cD2
JWT_SECRET=

# Refresh Token Secret (REQUIRED - Phase 3.8)
# Generate a secure secret with: openssl rand -base64 32
# NEVER use a weak secret or commit the real secret
REFRESH_SECRET=
